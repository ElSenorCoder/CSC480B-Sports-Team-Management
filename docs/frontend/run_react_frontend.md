# How to Run the React Frontend

1. Open the terminal in Visual Studio Code with `Ctrl + J`
2. Navigate to the frontend directory: `cd client`
3. Install node dependencies: `npm install`
4. Start the local React development server: `npm run dev`
5. Open [http://localhost:5173](http://localhost:5173) in your browser to view the application.
