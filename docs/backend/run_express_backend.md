# How to Run the Express Backend

1. Open the terminal in Visual Studio Code with `Ctrl + J`.
2. Navigate to the backend directory: `cd server`
3. Install the dependencies (only required the first time): `npm install`
4. Start the backend server: `nodemon`
5. Open your browser and go to: `http://localhost:3001/`
