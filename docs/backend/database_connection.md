# Database Connection Guide

1. Open **MySQL Workbench**.
2. Create a new local connection **(only required the first time)** using:
   - **Host:** `localhost` (127.0.0.1)
   - **Port:** `3306`
   - **Username:** `root`
   - **Password (for each team member):** Use the password you created when installing MySQL on your own computer.
